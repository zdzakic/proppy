/**
 * Re-exports OwnerFormValues from its canonical home in components/forms/OwnerForm.
 * Kept for backwards-compatibility so existing imports across the codebase don't need changing.
 */
export type { OwnerFormValues } from "@/components/forms/OwnerForm";
